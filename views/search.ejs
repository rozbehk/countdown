<%- include('../partials/header') %>


    <h2>Search</h2>
    <form action="/search" method="POST">
        <label>Title:</label>
        <input type="text" name="searchString">
        <select name="type" id="">
            <option value="movie">movie</option>
            <option value="tv">series</option>
        </select>
        <button>Search</button>  
    </form>
        <% searchResult.forEach(function(result){ %>
            <div class="episode-detail">
                <div>
                   <% if(result.poster_path != null ){ %>
                        <img class="poster" src="<%= `https://image.tmdb.org/t/p/original${result.poster_path}` %>" alt="">
                    <% }else{ %>
                        <img class="poster" src="/images/nopicture.jpg" alt="">
                    <% } %>
                   
                </div>
                <div>
                  <%=  result.title %>
                </div>
                <div>
                    <% if(type == 'movie'){ %>
                        <form action="/movies" method="POST">
                            <input type="hidden" name="movieId" value="<%=result.id%>">
                          <input type="submit" value="Add To Website">
                        </form>
                  <%  }else{ %>
                        <form action="/series" method="POST">
                            <input type="hidden" name="serieId" value="<%=result.id%>">
                          <input type="submit" value="Add To Website">
                        </form>
                  <%  } %>
                    
                </div>
                
            </div>
            
        <% }) %>

        
        <%- include('../partials/footer') %>